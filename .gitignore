# macOS
.DS_Store

# Xcode
build/
DerivedData/
# Release-Artefakte (xcarchive, exportiertes .app, DMG, appcast). Bewusst
# ausgenommen — alles, was scripts/release.sh produziert.
build/release/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
xcuserdata/
*.moved-aside
*.pbxuser
!default.pbxuser
*.perspectivev3
!default.perspectivev3

# Swift Package Manager
.build/
.swiftpm/
# Package.resolved wird bewusst eingecheckt (App-Repo, reproduzierbare Builds).

# Obj-C / C++ build artefacts
*.o
*.a
*.dylib

# Vendor build intermediates (TagLib/aubio/KeyFinder sources + build dirs).
# Die gebauten SetifyCore/Vendor/*.xcframework werden eingecheckt.
Vendor/TagLib/build/
Vendor/TagLib/src/
Vendor/aubio/build/
Vendor/aubio/src/
Vendor/KeyFinder/build/
Vendor/KeyFinder/src/

# Secrets / lokale Konfiguration
.env
*.local
secrets/

# Editor
.vscode/
.idea/
